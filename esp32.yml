esphome:
  name: esphome-build-action-esp32
  platform: ESP32
  board: esp32dev

# Enable logging
logger:

# Enable Home Assistant API
api:

ota:

wifi:
  # ssid: !secret wifi_ssid
  # password: !secret wifi_password

  # Enable fallback hotspot (captive portal) in case wifi connection fails
  ap:
    ssid: 'ESPHome Build'
    # password: 'P87dN2YcoPK5'

captive_portal:

binary_sensor:
  - platform: gpio
    id: button
    pin: 0
